import { Inter, Noto_Sans_Bengali } from "next/font/google";

export const inter = Inter({
    variable: "--font-inter",
    subsets: ["latin"]
});

export const noto_sans = Noto_Sans_Bengali({
    variable: "--font-noto-sans",
    subsets: ["latin"]
})